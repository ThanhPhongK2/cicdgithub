version: "3"
services:
  frontend:
    build:
      context: ./client
    ports:
      - "80:80"
    restart: always

  backend:
    build:
      context: ./server
    env_file:
      - ./server/.env
    ports:
      - "3000:3000"
    restart: always
