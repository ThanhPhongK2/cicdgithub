version: "3"
services:
  frontend:
    build:
      context: ./client
    ports:
      - "80:80"
    restart: always

  backend:
    build:
      context: ./server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env
    restart: always
